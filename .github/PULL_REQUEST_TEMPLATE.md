## Summary

<!-- 1-3 bullets: what this PR does, from the user's POV. -->

## Why

<!-- The problem this solves or the decision this implements. Link any
issue / Slack thread / Linear ticket. -->

## Test plan

<!-- Concrete steps a reviewer can follow to verify. Prefer "click X,
expect Y" over "I tested it." -->

- [ ] `npm run build` passes locally
- [ ] `npm test` passes locally
- [ ] Manually verified the golden path

## Required prod config

<!-- New env vars, dashboard settings (Supabase Auth, Stripe webhook,
R2 CORS), or external service config that ops needs to apply. Put
"None" if nothing. -->

## Migrations to apply

<!-- Migration numbers + brief description. Confirm they've been run
on Supabase before merging. Put "None" if no schema changes. -->
